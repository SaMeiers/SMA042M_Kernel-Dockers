# Everything else Docker/Droidspaces needs (NAMESPACES, PID_NS, NET_NS,
# UTS_NS, CGROUPS, MEMCG, full NETFILTER/NF_NAT/BRIDGE/VETH, OVERLAY_FS)
# is already =y in the stock a04e_defconfig. These two are the only
# ones stock leaves off:
CONFIG_USER_NS=y
CONFIG_CGROUP_DEVICE=y
